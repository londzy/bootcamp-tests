/*var isFromBellville = function(regNum){
 //var result = regNum;//.startsWith("CY");
  if(regNum.startsWith("CY")){
    return true;
   }
  else(regNum.startsWith("CJ")){
    return false;
   }
// return result;
};*/

var isFromBellvile = function(numbers){
  var regNum = numbers.startsWith("CY");
  return regNum;
};
